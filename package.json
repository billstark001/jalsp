{
  "name": "jalsp-workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:core": "pnpm --filter jalsp -r test",
    "test:cli": "pnpm --filter jalsp-cli -r test",
    "lint": "pnpm -r lint"
  }
}